@using Microsoft.AspNetCore.Identity
@using ClubestApp.Data.Models
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="User" asp-action="Profile" title="Manage">�������, @User.Identity.Name!</a>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link text-dark">�����</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-white link-nav" data-toggle="modal" data-target="#modalLoginForm" href="">�����������</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white link-nav" asp-area="Identity" asp-page="/Account/Login">����</a>
        </li>
    }
    <li class="nav-item">
        <form class="form-inline ml-auto">
            <div class="md-form input-group mb-3">
                <input type="text" class="form-control" placeholder="Recipient's username" aria-label="Search"
                       aria-describedby="MaterialButton-addon2">
                <div class="input-group-append">
                    <button class="btn btn-md btn-secondary m-0 px-3" type="submit" id="MaterialButton-addon2">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </div>
        </form>
    </li>
</ul>
